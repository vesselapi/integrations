import { PlatformClient } from '../../sdk/types';

const client: PlatformClient = {
  request: async (options, auth) => {
    // TODO: Implement request to hubspot
    // ky/axios/superagent/hubspot-sdk()
  },
};

export default client;
